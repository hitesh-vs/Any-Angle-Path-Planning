/**
 * Map Processor Header
 * This header file contains the declarations for the Map Processor Node,
 * which is responsible for converting raw lidar data into a 2D map.
 */

 #include <vector>
 #include <thread>

struct Point2D {
     float x; // X coordinate
     float y; // Y coordinate
 };

